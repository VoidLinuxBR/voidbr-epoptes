diff --git a/usr/lib/python3.14/site-packages/epoptes/ui/gui.py b/usr/lib/python3.14/site-packages/epoptes/ui/gui.py
index 720c958..b133d32 100644
--- a/usr/lib/python3.14/site-packages/epoptes/ui/gui.py
+++ b/usr/lib/python3.14/site-packages/epoptes/ui/gui.py
@@ -7,7 +7,7 @@ Epoptes GUI class.
 """
 from distutils.version import LooseVersion
 import os
-import pipes
+import shlex
 import random
 import socket
 import string
@@ -1059,7 +1059,7 @@ class EpoptesGui(object):
         # ${GUI_PY} must be unquoted, don't pass it as list
         if isinstance(command, list) and len(command) > 0:
             command = '%s %s' % (command[0], ' '.join(
-                [pipes.quote(str(x)) for x in command[1:]]))
+                [shlex.quote(str(x)) for x in command[1:]]))
 
         if (clients != [] or handles != []) and warning != '':
             if not self.confirmation_dialog(warning):
diff --git a/usr/share/epoptes/client-functions b/usr/share/epoptes/client-functions
index d54ba3b..df8a36a 100644
--- a/usr/share/epoptes/client-functions
+++ b/usr/share/epoptes/client-functions
@@ -260,9 +260,11 @@ shutdown() {
 # $2 = thumbshot height.
 thumbshot() {
     # TODO: remove compatibility fallback after a few versions
+    # VoidBR patch: thumbshot.py agora sabe capturar em Wayland
+    # (grim/gdbus/spectacle), entao nao pulamos mais o thumbshot
+    # so por causa de WAYLAND_DISPLAY estar definido.
     local executable
 
-    test -z "$WAYLAND_DISPLAY" || return 0
     if [ -f "./thumbshot.py" ]; then
         executable=./thumbshot.py
     else
@@ -378,11 +380,62 @@ message() {
     fi
 }
 
+# Cria uma "conexao reversa" para um servidor VNC que so sabe escutar
+# (como o wayvnc), replicando o que x11vnc -connect_or_exit faz nativamente:
+# o wayvnc sobe escutando so em localhost, e o socat disca pro servidor
+# (equivalente a "$1" = host:porta do Epoptes) fazendo o bridge dos bytes.
+# Parametros:
+# $1 = host:port do servidor (o "${GUI_IP}:porta" que o Epoptes manda).
+# $2 = "viewonly" (observar, sem controlar) ou "assist" (com controle).
+wayland_reverse_vnc() {
+    local target="$1" mode="$2" wport wpid spid
+
+    if ! which wayvnc >/dev/null 2>&1; then
+        echo "wayland_reverse_vnc: wayvnc nao encontrado, instale o pacote wayvnc" >&5
+        return 1
+    fi
+    if ! which socat >/dev/null 2>&1; then
+        echo "wayland_reverse_vnc: socat nao encontrado" >&5
+        return 1
+    fi
+
+    # Porta livre em localhost, escolhida pelo proprio kernel (porta 0).
+    wport=$(python3 -c '
+import socket
+s = socket.socket()
+s.bind(("127.0.0.1", 0))
+print(s.getsockname()[1])
+s.close()
+' 2>/dev/null)
+    if [ -z "$wport" ]; then
+        echo "wayland_reverse_vnc: nao consegui escolher uma porta livre" >&5
+        return 1
+    fi
+
+    if [ "$mode" = viewonly ]; then
+        wpid=$(background -p wayvnc -d 127.0.0.1 "$wport")
+    else
+        wpid=$(background -p wayvnc 127.0.0.1 "$wport")
+    fi
+
+    # Da um tempo pro wayvnc comecar a escutar antes do socat tentar.
+    sleep 0.3
+
+    spid=$(background -p socat "TCP:$target" "TCP:127.0.0.1:$wport")
+
+    xprop_set EPOPTES_WAYVNC_PID "$wpid"
+    xprop_set EPOPTES_VNCVIEWER_PID "$spid"
+}
+
 # Connect to the server to be monitored.
 # Parameters:
 # $1 = host:port.
 get_monitored() {
-    background x11vnc -q -nopw -noshm -24to32 -viewonly -connect_or_exit "$1"
+    if [ -n "$WAYLAND_DISPLAY" ]; then
+        wayland_reverse_vnc "$1" viewonly
+    else
+        background x11vnc -q -nopw -noshm -24to32 -viewonly -connect_or_exit "$1"
+    fi
 }
 
 # Connect to the server to get assistance.
@@ -390,7 +443,11 @@ get_monitored() {
 # $1 = host:port.
 # $2 = grab keyboard and mouse.
 get_assisted() {
-    background x11vnc -q -nopw -noshm -24to32 ${2:+-grabptr -grabkbd} -connect_or_exit "$1"
+    if [ -n "$WAYLAND_DISPLAY" ]; then
+        wayland_reverse_vnc "$1" assist
+    else
+        background x11vnc -q -nopw -noshm -24to32 ${2:+-grabptr -grabkbd} -connect_or_exit "$1"
+    fi
 }
 
 # Deactivate the screensaver, in order for the users to watch a broadcast.
@@ -468,7 +525,7 @@ receive_broadcast() {
 # The vnc clients should automatically exit when the server is killed.
 # Unfortunately, that isn't always true, so try to kill them anyway.
 stop_receptions() {
-    local pid
+    local pid wpid
 
     unset receiving_broadcast
     ensure_display
@@ -477,6 +534,11 @@ stop_receptions() {
         kill "$pid"
         xprop_unset EPOPTES_VNCVIEWER_PID
     fi
+    wpid=$(xprop_get EPOPTES_WAYVNC_PID)
+    if [ -n "$wpid" ]; then
+        kill "$wpid"
+        xprop_unset EPOPTES_WAYVNC_PID
+    fi
 }
 
 # Open a root terminal inside the X session.
